// Placeholder for the Drizzle schema and database client backing
// packages/auth's `drizzleAdapter`. Populated by the auth-core follow-up PR
// on this release branch — this module exists so the workspace, its
// tooling, and its tests are wired before any schema lands.
export const DB_PACKAGE_PLACEHOLDER = true
