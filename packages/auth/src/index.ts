// Placeholder for the Better Auth server instance and the config-driven
// provider registry (see the "Pluggable Signup & Login System" blueprint,
// section "The provider registry"). Populated by the auth-core follow-up PR
// on this release branch — this module exists so the workspace, its
// tooling, and its tests are wired before any auth logic lands.
export const AUTH_PACKAGE_PLACEHOLDER = true
