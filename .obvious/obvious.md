# zerolatent/obvious

## Status: empty repository

This repository is currently **empty**. It contains no commits, no tracked
files (`git ls-files` returns nothing), no dependency manifests, no
Dockerfile / Compose file / Makefile, and no application source code. The
GitHub API confirms the repository is empty (`isEmpty: true`) and has no
default branch yet.

Because there is no code, nothing is claimed here about:

- stack, languages, or runtimes
- install / dev / test / lint / build commands
- ports, services, or environment variables
- how to run the app locally

These sections — stack, commands, codebase map, local verification, and
snapshot info — will be filled in by the next setup run once real code lands
in this repository.

## Local verification

Not applicable. There is no application to install, start, or verify yet.
