# The auth server this app talks to. Mobile is always cross-origin, so this is
# real configuration — Expo inlines EXPO_PUBLIC_* at build time.
# Unset falls back to http://localhost:4000 (examples/api's default port).
EXPO_PUBLIC_AUTH_BASE_URL=http://localhost:4000
